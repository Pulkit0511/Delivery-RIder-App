import { View, Text } from "react-native";

// Current order screen component

export default CurrentOrderScreen = (props) => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Current Order Screen</Text>
    </View>
  );
};
